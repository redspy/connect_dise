# 구현 이슈/개선 포인트

## 1) 로비 스타일 규칙 우선순위
- 위치: `index.html` 인라인 스타일
- 현상: 공통 스타일과 충돌 시 의도한 레이아웃이 깨질 수 있어 일부 규칙에 `!important` 사용
- 개선: 로비 전용 스타일을 별도 CSS 모듈로 분리해 우선순위 의존도 축소

## 2) 대형 번들 경고
- 위치: `npm run build` 결과 (`>500kB chunk` 경고)
- 현상: 일부 게임 번들이 큼
- 개선: 동적 import 또는 manualChunks 설정으로 초기 로딩 부담 완화
